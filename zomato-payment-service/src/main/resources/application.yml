spring:
  application:
    name: zomato-payment-service
  datasource:
    url: jdbc:mysql://localhost:3306/zomato_payment_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
   hibernate.ddl-auto: update
   generate-ddl: false
   show-sql: true   
   database-platform: org.hibernate.dialect.MySQL8Dialect 
   database: mysql
  data:
    jpa:
      repositories:
        enabled: true
   
server:  port: 8082